# The ranker itself (rank.py and everything in khoj/) uses ONLY the Python
# standard library. The Stage 3 reproduction needs nothing installed.
#
# The two packages below are only for the optional extras:
#   streamlit  -> the hosted sandbox/demo app (app.py)
#   pytest     -> running the test suite (tests/)
# They are not imported by the ranking path, so installing them does not affect
# reproduction of submission.csv.

streamlit>=1.30
pytest>=7.0
